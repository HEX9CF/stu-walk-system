<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.WxUsersMapper">
    <update id="changeUsername">
        update wx_users set name = #{afterUsername} where id = #{id}
    </update>
    <update id="changeSex">
        update wx_users set sex = #{sex} where id = #{id}
    </update>
    <update id="changeIntroduction">
        update wx_users set introduction = #{introduction} where id = #{id}
    </update>
</mapper>